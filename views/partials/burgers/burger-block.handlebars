<li>

  {{burger_name}}

    <button class="change-devour" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}}DEVOUR!{{else}}EAT LATER!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">Delete Order!</button>

</li>